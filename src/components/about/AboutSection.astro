---
import { Image } from "astro:assets";
---

<section>
  <div class="content-wrapper">

    <div class="description-wrapper">
      <h2>Kalina Zajączkowska</h2>
      <div class="paragraphs">
        <p>założycielka i prezes Stowarzyszenia przyjaciół z&nbsp;autyzmem Nie&nbsp;z&nbsp;Tej&nbsp;Bajki. </p>
        <p>Dyplomowany neurologopeda, oligofrenopedagog, terapeuta integracji sensorycznej oraz komunikacji alternatywnej. Prywatnie matka dorosłego syna ze spektrum autyzmu.</p>
      </div>
    </div>
    
    <div class="image-wrapper">
      <Image
        src={"/images/about_image.png"}
        width={1000}
        height={2000}
        alt={"Kalina Zajączkowska"}
      />
    </div>
    
  </div>
</section>

<style>
  .content-wrapper {
    display: flex;
    max-width: var(--section-max-width);
    margin: 0 auto;
    gap: 2rem;
  }
  .description-wrapper {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 2rem;
  }

  .description-wrapper, .image-wrapper {
    display: flex;
    flex: 1;
  }
  .image-wrapper, .image-wrapper-mobile {
    border-radius: 1.25rem;
    overflow: hidden;
    background-color: rgb(233, 233, 233);  /* TODO: Delete after correct image implementation */
  }
  .image-wrapper img, .image-wrapper-mobile img {
    height: auto;
    width: 100%;
  }

  .paragraphs {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .paragraphs p {
    font-size: 1.5rem;
  }


/* MEDIA QUERIES */
/* Large screens (1200px and below) */
@media only screen and (max-width: 1200px) {
  .paragraphs p {
    font-size: 1.25rem;
  }
}
/* Medium screens (768px and below) */
@media only screen and (max-width: 768px) {
  .image-wrapper {
    height: 100%;
  }
  .paragraphs p {
    font-size: 1.125rem;
  }
}
/* Small screens (485px and below) */
@media only screen and (max-width: 485px) {
  .content-wrapper {
    flex-direction: column;
  }
  .description-wrapper {
    gap: 1rem;
  }
  .paragraphs p {
    font-size: 1rem;
  }
}
</style>